import React from "react";

const headerStyles = {
    fontFamily: 'Arial',
    display: 'flex',
    justifyContent: 'center',
    color: '#3f51b5'
}

const spinnerStyles = {
    fontSize: '3em',
    display: 'flex',
    justifyContent: 'center',
    color: '#3f51b5'
}

const paginateButtonWrapperStyles = {
    display: 'flex',
    justifyContent: 'center',
    margin: '10px 10px'
}
const paginateButtonStyles = {
    display: 'flex',
    marginRight: '10px'
}

const errorMessageStyles = {
    color: 'red',
    display: 'flex',
    justifyContent: 'center',
    fontSize: '1em',
    fontFamily: 'Arial'
}

const wrapperStyles = {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: '10px',
};

const inputStyles = {
    display: 'flex',
    width: '500px',
    height: '30px',
    marginRight: '10px'
};

export {
    headerStyles,
    spinnerStyles,
    paginateButtonWrapperStyles,
    paginateButtonStyles,
    errorMessageStyles,
    wrapperStyles,
    inputStyles
}
